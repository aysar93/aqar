def register(engine):

    # Font Size

    engine.add(
        r"fontSize\s*:\s*30",
        "fontSize: AqarText.hero(context)",
    )

    engine.add(
        r"fontSize\s*:\s*28",
        "fontSize: AqarText.display(context)",
    )

    engine.add(
        r"fontSize\s*:\s*26",
        "fontSize: AqarText.display(context)",
    )

    engine.add(
        r"fontSize\s*:\s*24",
        "fontSize: AqarText.headline(context)",
    )

    engine.add(
        r"fontSize\s*:\s*22",
        "fontSize: AqarText.title(context)",
    )

    engine.add(
        r"fontSize\s*:\s*20",
        "fontSize: AqarText.title(context)",
    )

    engine.add(
        r"fontSize\s*:\s*18",
        "fontSize: AqarText.subtitle(context)",
    )

    engine.add(
        r"fontSize\s*:\s*17",
        "fontSize: AqarText.subtitle(context)",
    )

    engine.add(
        r"fontSize\s*:\s*16",
        "fontSize: AqarText.body(context)",
    )

    engine.add(
        r"fontSize\s*:\s*15",
        "fontSize: AqarText.body(context)",
    )

    engine.add(
        r"fontSize\s*:\s*14",
        "fontSize: AqarText.bodySmall(context)",
    )

    engine.add(
        r"fontSize\s*:\s*13",
        "fontSize: AqarText.caption(context)",
    )

    engine.add(
        r"fontSize\s*:\s*12",
        "fontSize: AqarText.caption(context)",
    )

    engine.add(
        r"fontSize\s*:\s*11",
        "fontSize: AqarText.overline(context)",
    )

    engine.add(
        r"fontSize\s*:\s*10",
        "fontSize: AqarText.overline(context)",
    )